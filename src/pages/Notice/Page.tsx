import React from 'react';
import MonoTemplate from '@components/templates/mono-template';

const Notice = () => {
    return <MonoTemplate mainSection={<div>공지사항 페이지</div>} />;
};

export default Notice;
