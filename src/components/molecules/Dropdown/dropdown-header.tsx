import React from 'react';

interface Props {
    header: React.ReactNode;
}

const DropdownHeader = ({ header }: Props) => {
    return <div>dropdown-header</div>;
};

export default DropdownHeader;
