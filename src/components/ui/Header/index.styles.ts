import styled from "@emotion/styled";
import theme from "@styles/theme";
import { LogoProps, HeaderProps } from "./index.types";

export const Logo = styled.div<LogoProps>`
    fontSize='xxl',
    fontWeight=700,
    lineHeight='xxl'
`